# 🤖 Настройка ИИ для диагностики

## Обзор

Система автоматически генерирует диагностику студента с помощью искусственного интеллекта. Эксперт только вводит базовые данные, а ИИ заполняет:
- Комментарии трекера (общий вывод)
- Список университетов (8 штук с разными странами)
- Дорожную карту (приоритеты и задачи с дедлайнами)
- Рекомендации по инструментам развития

## Варианты настройки

### Вариант 1: OpenAI API (Рекомендуется)

**Преимущества:**
- Высокое качество генерации
- Быстрая работа
- Поддержка GPT-4 и GPT-3.5

**Настройка:**

1. Получите API ключ на [platform.openai.com](https://platform.openai.com/api-keys)

2. Откройте `ai-diagnostic.js` и найдите секцию `AI_CONFIG`:

```javascript
const AI_CONFIG = {
    provider: 'openai',
    apiKey: 'sk-your-api-key-here', // Вставьте ваш ключ
    model: 'gpt-4-turbo-preview', // или 'gpt-3.5-turbo' для экономии
    baseURL: 'https://api.openai.com/v1',
};
```

3. Сохраните файл

**Стоимость:** ~$0.01-0.03 за диагностику (зависит от модели)

---

### Вариант 2: Anthropic Claude API

**Преимущества:**
- Очень высокое качество анализа
- Отличное понимание контекста
- Безопасность данных

**Настройка:**

1. Получите API ключ на [console.anthropic.com](https://console.anthropic.com/)

2. Обновите `AI_CONFIG`:

```javascript
const AI_CONFIG = {
    provider: 'anthropic',
    apiKey: 'sk-ant-your-api-key-here',
    model: 'claude-3-opus-20240229', // или 'claude-3-sonnet-20240229'
    baseURL: 'https://api.anthropic.com/v1',
};
```

**Стоимость:** ~$0.015-0.075 за диагностику

---

### Вариант 3: Локальный сервер (Бесплатно)

**Преимущества:**
- Полностью бесплатно
- Данные не покидают ваш компьютер
- Полный контроль

**Недостатки:**
- Требует мощный компьютер
- Может быть медленнее

**Настройка с Ollama:**

1. Установите [Ollama](https://ollama.ai/)

2. Скачайте модель:
```bash
ollama pull llama2
# или
ollama pull mistral
```

3. Запустите Ollama сервер (обычно запускается автоматически)

4. Обновите `AI_CONFIG`:

```javascript
const AI_CONFIG = {
    provider: 'local',
    baseURL: 'http://localhost:11434/v1', // Стандартный порт Ollama
};
```

**Альтернативы:**
- **LM Studio** (Windows/Mac): [lmstudio.ai](https://lmstudio.ai/)
- **Ollama Web UI**: Веб-интерфейс для Ollama
- **LocalAI**: Сервер для локальных моделей

---

## Использование

### Для эксперта:

1. Откройте диагностику
2. Заполните базовые данные:
   - Имя и фамилия
   - Школа/колледж
   - Бюджет
   - Достижения (хотя бы 1-2)
   - Рейтинги компетенций (выберите селекты)
3. Нажмите кнопку **"🤖 Сгенерировать с ИИ"**
4. Дождитесь генерации (10-30 секунд)
5. Проверьте и отредактируйте сгенерированные данные
6. Сохраните PDF

### Что генерирует ИИ:

#### 1. Комментарии трекера
- Анализ сильных и слабых сторон
- Оценка потенциала поступления
- Персонализированные рекомендации

#### 2. Список университетов (8 штук)
- Разные страны (США, UK, Канада, Европа, Азия)
- Учитывает бюджет студента
- Учитывает рейтинги компетенций
- Реалистичные варианты

#### 3. Дорожная карта (8-10 задач)
- Конкретные измеримые задачи
- Приоритеты от 1 до 10
- Дедлайны на ближайшие 6-12 месяцев
- Учитывает слабые стороны профиля

#### 4. Инструменты развития
- Автоматически отмечает 5-7 важных инструментов
- На основе анализа профиля
- Можно изменить вручную

---

## Безопасность

### Рекомендации:

1. **Не коммитьте API ключи в Git:**
   ```bash
   # Добавьте в .gitignore
   ai-diagnostic.js
   # Или используйте переменные окружения
   ```

2. **Используйте переменные окружения:**
   ```javascript
   // В production используйте:
   apiKey: process.env.OPENAI_API_KEY || ''
   ```

3. **Ограничьте доступ:**
   - Используйте API ключи с ограничениями по IP
   - Установите лимиты расходов
   - Регулярно ротируйте ключи

4. **Для продакшена:**
   - Создайте backend API endpoint
   - Храните ключи на сервере
   - Добавьте rate limiting

---

## Настройка промпта

Если нужно изменить поведение ИИ, отредактируйте функцию `createAIPrompt()` в `ai-diagnostic.js`:

```javascript
function createAIPrompt(data) {
    // Здесь можно:
    // - Изменить тон ответов
    // - Добавить дополнительные инструкции
    // - Изменить формат вывода
    // - Добавить примеры
}
```

---

## Troubleshooting

### Ошибка: "API ключ не установлен"
- Проверьте, что вы установили `AI_CONFIG.apiKey`
- Убедитесь, что ключ правильный (начинается с `sk-` для OpenAI)

### Ошибка: "Network error"
- Проверьте интернет-соединение
- Для локального API убедитесь, что сервер запущен
- Проверьте CORS настройки

### Ошибка: "Invalid JSON response"
- ИИ иногда добавляет текст вокруг JSON
- Код автоматически извлекает JSON, но если проблема повторяется:
  - Попробуйте другую модель
  - Уменьшите `temperature` в запросе
  - Добавьте более строгие инструкции в промпт

### Медленная генерация
- Используйте более быструю модель (gpt-3.5-turbo вместо gpt-4)
- Для локального API используйте меньшую модель
- Проверьте скорость интернета

### Низкое качество генерации
- Используйте более мощную модель (GPT-4, Claude Opus)
- Улучшите промпт (добавьте примеры)
- Убедитесь, что входные данные заполнены полностью

---

## Примеры использования

### Тестовая диагностика

```javascript
// В консоли браузера можно протестировать:
const testData = {
    name: "Иван Иванов",
    school: "Лицей №1",
    budget: { amount: "50000", currency: "$" },
    achievements: ["Победитель олимпиады по математике", "Лидер школьного совета"],
    ratings: {
        academic: 2,
        extracurricular: 3,
        intellectual: 2,
        essay: 4,
        recommendations: 3,
        eq: 3
    }
};

// Создать промпт
const prompt = createAIPrompt(testData);
console.log(prompt);

// Вызвать ИИ (требует API ключ)
generateDiagnosticWithAI();
```

---

## Дальнейшие улучшения

### Возможные расширения:

1. **Кэширование результатов** - уже реализовано
2. **История генераций** - сохранять предыдущие версии
3. **A/B тестирование** - сравнивать разные модели
4. **Fine-tuning модели** - обучить на ваших данных
5. **Мультиязычность** - поддержка английского/узбекского
6. **Интеграция с базой университетов** - использовать реальные данные
7. **Валидация результатов** - проверка на реалистичность

---

## Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12) на ошибки
2. Убедитесь, что API ключ правильный
3. Проверьте баланс API аккаунта
4. Попробуйте другую модель

---

## Лицензия и использование

⚠️ **Важно:** При использовании OpenAI/Anthropic API убедитесь, что:
- Вы соблюдаете их Terms of Service
- Данные студентов обрабатываются в соответствии с GDPR/законодательством
- Вы информируете пользователей об использовании ИИ
