# 🔧 Решение проблем с ИИ диагностикой

## Проблема: Не вижу кнопку "🤖 Сгенерировать с ИИ"

### Решение 1: Очистить кэш браузера

**Chrome/Edge:**
1. Нажмите `Ctrl+Shift+Delete` (Windows) или `Cmd+Shift+Delete` (Mac)
2. Выберите "Кэшированные изображения и файлы"
3. Нажмите "Очистить данные"

**Или жесткая перезагрузка:**
- `Ctrl+F5` (Windows) или `Cmd+Shift+R` (Mac)

### Решение 2: Проверить консоль браузера

1. Откройте диагностику
2. Нажмите `F12` (или `Cmd+Option+I` на Mac)
3. Перейдите на вкладку "Console"
4. Ищите сообщения:
   - ✅ "Инициализация ИИ диагностики..."
   - ✅ "Кнопка ИИ найдена"
   - ❌ "Кнопка ИИ не найдена!" - значит проблема в HTML

### Решение 3: Проверить загрузку файлов

В консоли браузера выполните:
```javascript
// Проверить, загружен ли скрипт
console.log(typeof generateDiagnosticWithAI);

// Проверить, есть ли кнопка
console.log(document.getElementById('aiGenerateBtn'));

// Проверить, есть ли стили
const btn = document.getElementById('aiGenerateBtn');
if (btn) {
    console.log('Стили кнопки:', window.getComputedStyle(btn));
}
```

### Решение 4: Проверить путь к файлу

Убедитесь, что файл `ai-diagnostic.js` находится в той же папке, что и `index.html`.

Проверьте в HTML (должно быть в конце перед `</body>`):
```html
<script src="ai-diagnostic.js"></script>
```

### Решение 5: Проверить порядок загрузки скриптов

В `index.html` скрипты должны идти в таком порядке:
```html
<script src="script.js"></script>
<script src="ai-diagnostic.js"></script>
```

---

## Проблема: Кнопка видна, но не работает

### Решение 1: Проверить ошибки в консоли

Откройте консоль (`F12`) и нажмите кнопку. Если есть ошибки - они появятся в консоли.

### Решение 2: Проверить API ключ

В консоли выполните:
```javascript
console.log(AI_CONFIG);
```

Если `apiKey` пустой - установите его в `ai-diagnostic.js`.

### Решение 3: Проверить инициализацию

В консоли выполните:
```javascript
// Принудительно инициализировать
if (typeof initAIDiagnostic === 'function') {
    initAIDiagnostic();
} else {
    console.error('Функция initAIDiagnostic не найдена!');
}
```

---

## Проблема: Кнопка не видна визуально

### Решение 1: Проверить CSS

В консоли выполните:
```javascript
const btn = document.getElementById('aiGenerateBtn');
if (btn) {
    const styles = window.getComputedStyle(btn);
    console.log('Display:', styles.display);
    console.log('Visibility:', styles.visibility);
    console.log('Opacity:', styles.opacity);
    console.log('Width:', styles.width);
    console.log('Height:', styles.height);
}
```

Если `display: none` - значит стили не загружены или переопределены.

### Решение 2: Проверить загрузку CSS

Убедитесь, что `styles.css` подключен в `<head>`:
```html
<link rel="stylesheet" href="styles.css">
```

### Решение 3: Временное решение - добавить inline стили

Если стили не работают, можно временно добавить в HTML:
```html
<button id="aiGenerateBtn" 
        class="btn btn-ai" 
        style="display: inline-flex; padding: 0.75rem 1.5rem; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; border: none; border-radius: 12px; cursor: pointer;">
    🤖 Сгенерировать с ИИ
</button>
```

---

## Проблема: Ошибка "API ключ не установлен"

### Решение:

1. Откройте `ai-diagnostic.js`
2. Найдите строку с `apiKey: ''`
3. Вставьте ваш API ключ:
   ```javascript
   apiKey: 'sk-ваш-ключ-здесь',
   ```
4. Сохраните файл
5. Обновите страницу с очисткой кэша (`Ctrl+F5`)

---

## Проблема: Ошибка сети при вызове API

### Решение 1: Проверить интернет

Убедитесь, что есть подключение к интернету.

### Решение 2: Проверить CORS (для локального API)

Если используете локальный API (Ollama), убедитесь, что CORS разрешен.

Для Ollama добавьте в настройки или используйте прокси.

### Решение 3: Проверить URL API

В консоли проверьте:
```javascript
console.log('API URL:', AI_CONFIG.baseURL);
console.log('Provider:', AI_CONFIG.provider);
```

---

## Быстрая диагностика

Выполните в консоли браузера:

```javascript
// Полная проверка
function checkAISetup() {
    console.log('=== Проверка ИИ диагностики ===');
    
    // 1. Проверка файла
    console.log('1. Скрипт загружен:', typeof generateDiagnosticWithAI !== 'undefined');
    
    // 2. Проверка кнопки
    const btn = document.getElementById('aiGenerateBtn');
    console.log('2. Кнопка найдена:', !!btn);
    if (btn) {
        console.log('   - Видима:', btn.offsetWidth > 0 && btn.offsetHeight > 0);
        console.log('   - Стили:', window.getComputedStyle(btn).display);
    }
    
    // 3. Проверка статус-бара
    const status = document.getElementById('aiStatus');
    console.log('3. Статус-бар найден:', !!status);
    
    // 4. Проверка конфигурации
    console.log('4. Конфигурация:', AI_CONFIG);
    console.log('   - Provider:', AI_CONFIG.provider);
    console.log('   - API Key установлен:', !!AI_CONFIG.apiKey);
    
    // 5. Проверка модального окна
    const modal = document.getElementById('diagnosticModal');
    console.log('5. Модальное окно найдено:', !!modal);
    
    console.log('=== Конец проверки ===');
}

checkAISetup();
```

---

## Если ничего не помогло

1. **Проверьте версию файлов:**
   - Убедитесь, что все файлы сохранены
   - Проверьте дату изменения файлов

2. **Попробуйте в другом браузере:**
   - Chrome
   - Firefox
   - Safari

3. **Проверьте на локальном сервере:**
   - Используйте Live Server в VS Code
   - Или `python -m http.server` в папке проекта

4. **Включите подробное логирование:**
   - В консоли выполните: `localStorage.setItem('ai_debug', 'true')`
   - Обновите страницу
   - Проверьте консоль на дополнительные сообщения

---

## Контакты для поддержки

Если проблема не решена, предоставьте:
1. Скриншот консоли браузера (F12)
2. Результат выполнения `checkAISetup()`
3. Версию браузера
4. Операционную систему
