# ✅ Проверка работы ИИ диагностики

## Быстрая проверка (30 секунд)

1. **Откройте сайт** в браузере
2. **Нажмите F12** (открыть консоль разработчика)
3. **Нажмите "Диагностика"** на сайте
4. **Проверьте консоль** - должны быть сообщения:
   ```
   🔍 Инициализация ИИ диагностики...
   ✅ Кнопка ИИ найдена
   ✅ Обработчик события добавлен
   ```
5. **Проверьте визуально** - в правом верхнем углу диагностики должна быть кнопка:
   **🤖 Сгенерировать с ИИ**

## Если кнопка не видна

### Вариант 1: Очистить кэш
- `Ctrl+Shift+Delete` → Очистить кэш
- Или `Ctrl+F5` (жесткая перезагрузка)

### Вариант 2: Проверить в консоли
Выполните в консоли браузера:
```javascript
// Проверка кнопки
const btn = document.getElementById('aiGenerateBtn');
console.log('Кнопка:', btn);
if (btn) {
    console.log('Видима:', btn.offsetWidth > 0);
    console.log('Стили:', window.getComputedStyle(btn).display);
}
```

### Вариант 3: Ручная инициализация
В консоли выполните:
```javascript
initAI();
```

## Если кнопка видна, но не работает

1. **Проверьте API ключ** в консоли:
   ```javascript
   console.log(AI_CONFIG);
   ```
   Если `apiKey` пустой - установите его в `ai-diagnostic.js`

2. **Попробуйте нажать кнопку** и посмотрите ошибки в консоли

3. **Проверьте заполненные данные**:
   - Должно быть заполнено хотя бы имя и школа
   - Рейтинги можно оставить по умолчанию

## Всё работает? 🎉

Теперь вы можете:
1. Заполнить базовые данные студента
2. Нажать "🤖 Сгенерировать с ИИ"
3. Подождать 10-30 секунд
4. Проверить и отредактировать сгенерированные данные
5. Сохранить PDF

---

## Структура файлов (для справки)

```
impact-admissions/
├── index.html          ← Кнопка на строке 917
├── script.js           ← Инициализация при открытии модального окна
├── ai-diagnostic.js    ← Основной код ИИ (строка 1306 в index.html)
├── styles.css          ← Стили кнопки (.btn-ai)
└── AI_SETUP.md         ← Подробная инструкция по настройке
```
